"""Общие helpers для XLSX-экспортов.

Раньше стилизация шапки таблицы (Font + PatternFill + Alignment + freeze_panes)
дублировалась между ``services/directories.build_xlsx`` и ``api/analytics.py``
(см. audit [L9]). Здесь — единый источник: при ребрендинге (смена акцентного
цвета, шрифта) меняется только здесь.

``staff_xlsx.py`` оставлен как есть — там более сложная структура (title +
subtitle + dept-блок + динамические колонки), к общему случаю не приводится.
"""
